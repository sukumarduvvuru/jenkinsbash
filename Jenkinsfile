pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'hello world build stage'
            }
        }

        stage('test') {
            steps {
                echo 'hello test stage'
            }
        }

        stage('deploy') {
            steps {
                echo 'hello deploy'
            }
        }
    }
}
